html
    head
        title Search Results
        style.
            body {
                background-color: #1a1a1a;
                color: #ffffff;
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 20px;
                min-height: 800px;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: flex-start;
                padding-top: 150px;
            }
            h1 {
                color: #2d5a27;
                text-align: center;
                margin-bottom: 20px;
            }
            .container {
                width: 800px;
                margin: 0 auto;
                padding: 30px;
                background-color: #2d5a27;
                border-radius: 8px;
                box-shadow: 0 0 10px rgba(0,0,0,0.5);
            }
            table {
                width: 800px;
                border-collapse: collapse;
                margin: 0 auto;
                background-color: #333333;
            }
            th, td {
                border: 1px solid #1a1a1a;
                padding: 12px;
                text-align: left;
            }
            th {
                background-color: #1a1a1a;
                color: #ffffff;
            }
            tr:hover {
                background-color: #3d6a37;
            }
            .button-container {
                display: flex;
                justify-content: center;
                margin-top: 30px;
            }
            a {
                display: inline-block;
                padding: 10px 20px;
                background-color: #1a1a1a;
                color: #ffffff;
                text-decoration: none;
                border-radius: 4px;
            }
            a:hover {
                background-color: #333333;
            }
            .no-results {
                text-align: center;
                color: #ffffff;
                padding: 20px;
            }
    body
        h1 Search Results
        .container
            if results && results.length > 0
                table
                    thead
                        tr
                            th Company Name
                            th Ticker Symbol
                            th Price
                    tbody
                        each company in results
                            tr
                                td= company.company
                                td= company.ticker
                                td $#{company.price}
            else
                p.no-results No results found. Please try a different search term.
            .button-container
                a(href="/") Back to Search 